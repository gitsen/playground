syntax = "proto3";

package chat;

message ChatRequest {
    string message = 1;
}

message ChatResponse {
    string message = 2;
}

service Chat {
    rpc Talk(stream ChatRequest) returns (stream ChatResponse) {}
}